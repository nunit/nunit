# Copilot Instructions

## Project Guidelines
- When refactoring for design patterns, preserve existing performance optimizations (e.g., O(n+m) dictionary-based paths) unless explicitly asked to remove them.
- Use public Execute(...) entry points for TextRunner tests instead of setting private fields via reflection or forcing parameters directly.

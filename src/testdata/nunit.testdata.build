<?xml version="1.0"?>
<project name="TestData" default="build" basedir=".">

  <target name="build">
    <exec program="${build.command}" verbose="true">
        <arg line='"nunit.testdata-${msbuild.proj.suffix}.csproj"' />
        <arg line="/property:Configuration=${build.config}" />
        <arg line="/property:OutputPath=${current.build.dir}" />
        <arg value="/target:Rebuild" />
        <arg value="/verbosity:normal" />
    </exec>
  
  </target>

  <target name="package">
    <copy todir="${package.src.dir}/testdata">
      <fileset>
        <include name="*.cs"/>
        <include name="*.csproj"/>
        <include name="nunit.testdata.build"/>
        <include name="nunitlite.testdata.build"/>
      </fileset>
    </copy>
  </target>

</project>
